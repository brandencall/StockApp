/*
Deployment script for StockDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "StockDB"
:setvar DefaultFilePrefix "StockDB"
:setvar DefaultDataPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"
:setvar DefaultLogPath "C:\Program Files\Microsoft SQL Server\MSSQL16.MSSQLSERVER\MSSQL\DATA\"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
PRINT N'Creating Procedure [dbo].[spFinancialDataApi_GetAnnual]...';


GO
CREATE PROCEDURE [dbo].[spFinancialDataApi_GetAnnual]
	@stockId int
AS
begin
	set nocount on;

	WITH RankedFinances AS (
    SELECT 
        s.Ticker,
        s.Name,
        fa.Title,
        fa.Label,
        u.Type,
        ff.StartDate,
        ff.EndDate,
        ff.Filed,
        ff.Frame,
        ff.FiscalYear,
        ff.Form,
        cv.Value AS CurrencyValue,
        sv.Value AS SharesValue,
        cpsv.Value AS CurrencyPerShareValue,
        ROW_NUMBER() OVER (
            PARTITION BY fa.Title, ff.FiscalYear 
            ORDER BY ff.EndDate DESC
        ) AS RowNum
    FROM
        dbo.StockFinances sf
    INNER JOIN dbo.Stocks s ON sf.StockId = s.Id
    INNER JOIN dbo.FinancialAttributes fa ON sf.FinancialAttributeId = fa.Id
    INNER JOIN dbo.FinancialFacts ff ON sf.FinancialFactId = ff.Id
    INNER JOIN dbo.Units u ON ff.UnitId = u.Id
    LEFT JOIN dbo.CurrencyValues cv ON sf.FinancialFactId = cv.FinancialFactId
    LEFT JOIN dbo.ShareValues sv ON sf.FinancialFactId = sv.FinancialFactId
    LEFT JOIN dbo.CurrencyPerShareValues cpsv ON sf.FinancialFactId = cpsv.FinancialFactId
    WHERE 
		fa.Title in (
		'RevenueFromContractWithCustomerExcludingAssessedTax', 'Revenues', 'OperatingIncomeLoss', 'NetIncomeLoss', 'InventoryNet',
		'AccountsReceivableNetCurrent', 'AssetsCurrent', 'LiabilitiesCurrent', 'CommonStocksIncludingAdditionalPaidInCapital',
		'StockholdersEquity', 'Goodwill', 'InventoryNet', 'CashAndCashEquivalentsAtCarryingValue', 'OperatingExpenses', 'CostAndExpenses'
		)
		AND s.Id = @stockId 
		AND ff.Form LIKE '%10-K%'
)
SELECT 
    Ticker,
    Name,
    Title,
    Label,
    Type,
    StartDate,
    EndDate,
    Filed,
    Frame,
    FiscalYear,
    Form,
    CurrencyValue,
    SharesValue,
    CurrencyPerShareValue
FROM 
    RankedFinances
WHERE 
    RowNum = 1
ORDER BY 
    Title, FiscalYear DESC, EndDate DESC;

end
GO
PRINT N'Update complete.';


GO
